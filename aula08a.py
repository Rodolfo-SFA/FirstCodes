from math import sqrt, trunc
num = int(input('Digite um número:'))
raiz = sqrt(num)
print('A raiz de {} é {}'.format(num, trunc(raiz)))

# MATH.CEIL() arredonda para cima, MATH.FLOOR() arredonda para baixo
# MATH.TRUNC() trunca o numero
